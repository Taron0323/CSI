# A100 formal-launch P0/P1 ledger

Date: 2026-08-09 (Asia/Shanghai)

Baseline: `origin/main@790053c44ac5cb3792ec87f72554bf2bb5ac42c4`

This ledger covers only issues that can block the first authenticated formal
gate chain or invalidate its evidence. It does not promote smoke, fixture, or
candidate artifacts to scientific evidence.

| ID | Severity | Reproduction/evidence | Impact | Minimal repair and acceptance | State |
|---|---|---|---|---|---|
| `HOTPATH-RESOURCE-001` | P1 code | `test_resource_localization_authentication_is_constant_in_result_rows` runs 1 and 10,000 result rows while asserting zero inner-loop file hashes and evidence-context calls. | Historical `O(rows * dataset/runtime)` cost made controls impractical. | Compute evidence once at the control boundary and pass immutable values into localization evaluation. The targeted test must pass. | `CLOSED_ON_BASELINE`: 1 test passed locally on 2026-08-09. |
| `RT-INDEPENDENCE-001` | P0 science/code | RT calibration adversarial tests cover renamed IDs, copied source bytes under a new filename, payload/source mismatch, canonical key order, equal measurements from distinct physical units, and the same raw unit under another wrapper. | Fit/validation reuse could falsely support C11. | Bind authenticated source asset, batch, record and raw-unit identity in the outer runner; recompute canonical payload identity; reject identity overlap without rejecting independent equal measurements. | `CLOSED_ON_BASELINE`: 5 adversarial tests passed locally on 2026-08-09. |
| `DATA-VISIBILITY-001` | P0 data/protocol | Candidate `e8903430...d2e` passes `inspect-data` with 34 independent base-map clusters, 34 banks, 4 worlds, 256 positions and both target cities. The digest inventory is in `artifacts/m4_llvm22_candidate_v1/candidate_evidence.json`; the earlier `e5ec3d32...7847` static registry remains historical. | The old pathless candidate remains forbidden; the new candidate is usable only after the remaining formal gates pass. | V2 still aborts on pathless/all-zero units. The replacement inspection and 34-row verification inventory are repository-auditable. | `CLOSED_CANDIDATE_EVIDENCE_INGESTED` |
| `DATA-REGEN-001` | P0 data/runtime | Latest main plus approved LLVM 22.1.8 independently regenerates all 34 banks. The final gate and scene inventory pass all 16 comparison fields, all nine roles, `rtol=0`, `atol=0`, with no nonblocking failure. | Deterministic same-engine consistency is established for this candidate; this does not provide independent G8 physics evidence. | A content-bound portable receipt is moved and replayed successfully with all 34 rows. | `CLOSED_CANDIDATE_ZERO_TOLERANCE_PASS` |
| `DATA-REPLAY-REGISTRY-001` | P1 science/code | An adversarial replay changed every origin/runtime digest in a copied receipt and recomputed its sidecar hash; the unregistered receipt was previously accepted. | A handwritten receipt could impersonate the live LLVM 22 regeneration. | Non-fixture replay now requires the exact receipt and origin fields recorded in `m4_llvm22_candidate_v1/candidate_evidence.json`; the registered bundle passes across consumer source revisions and the forged receipt fails with `not registered`. | `CLOSED_IN_CODE_AND_ADVERSARIAL_REPLAY` |
| `DATA-METADATA-001` | P0 code/data | The old candidate claimed deterministic metadata despite failed regeneration. | A failed candidate could be mistaken for a formal input. | V2 keeps output at `scientific_use=CANDIDATE`; only the outer independent verifier can promote role evidence, and the loader rejects pathless/all-zero formal units. | `CLOSED_IN_CODE_OLD_DATA_QUARANTINED` |
| `PATH-ID-001` | P0 science/data | Scene 12, world 0, receiver 27 exposed an incomplete historical identity. | Rendering previously stopped before a complete 34-bank candidate could exist. | AoA/AoD remain frozen at `1e-5` rad, identical complete records are coalesced with power preserved, and digest collisions remain fatal. Latest-main generation and exact regeneration complete. | `CLOSED_CODE_AND_FULL_CANDIDATE_PASS` |
| `DATA-PROMOTION-001` | P0 code/protocol | The V2 generator emits `scientific_use=CANDIDATE`; promotion requires authenticated data verification followed by G1 and G2. | A candidate cannot enter factorial before the same-run qualification gates pass. | Candidate and fixture regression paths pass; `FORMAL_EXPERIMENT_ALLOWED` remains impossible until real G1 and G2 pass. | `CLOSED_IN_CODE_AND_REGRESSION` |
| `DATA-CLUSTER-001` | P0 science/data | Replaying bank selection on the six frozen OSM responses selected two Boston target extents whose centers were 244.567 m apart, overlapping by 2,926.79 m2. | Overlapping geographic regions could be mislabeled as independent base-map clusters and invalidate cluster-level inference. | Keep the frozen score order but admit only disjoint 256 m scene extents within a city. The synthetic selector regression and six-city frozen-OSM replay both pass; Boston's minimum selected center distance is now 289.848 m. | `CLOSED_IN_CODE_AND_FROZEN_OSM_REPLAY` |
| `SUITE-FINAL-001` | P1 code | PR #10 reports 368 local tests and deterministic dual builds; this branch adds focused verifier, runtime and launch-path checks plus a live 34-bank replay. | The pending PR still requires its own exact-head CI. | Push the focused change and require exact PR-head CI; do not transfer earlier status to the PR commit. | `LOCAL_TARGETED_PASS_PR_CI_PENDING` |
| `GPU-SCHEDULER-001` | P1 code/runtime | Handoff dual-device probe covered Endpoint on `cuda:0` and Alignment on `cuda:1`; current tests cover ordered 4 arms x 3 seeds scheduling and the full 12-job fixture/checkpoint/localization chain on CPU. | Stable per-device queuing and checkpoint portability are code-covered, but the destination A100 host must still report actual peak memory. | One serial queue per GPU, CPU model initialization, one resident model per device, CPU checkpoint copies, strict reload, and destination-host runtime preflight. | `CODE_CLOSED_HOST_PREFLIGHT_PENDING` |
| `CUDA12-DELIVERY-001` | P1 packaging/runtime | A100 evidence passes on PyTorch 2.5.1+cu121, CUDA 12.1 and NCCL 2.21.5. The failed CUDA 13 preload branch is not present in this change. | Main must reproduce the known-good A100 runtime. | Platform-selected Linux CUDA 12.1 hash lock, fresh setup entrypoint, runtime closure validation, and explicit A100 runbook. | `CLOSED_IN_CODE_HOST_REINSTALL_PENDING` |
| `G8-INTERFACE-001` | P0 science/code | The previous G8 runner rejected a Sionna primary dataset but only accepted a Sionna-specific command, scene loader, and runtime profile, so no genuinely independent engine could enter G8. | G8 remains externally blocked until a genuinely independent executable engine or controlled intervention is integrated. | Keep the precomputed independent-RT archive contract diagnostic-only for authenticated interchange and first-party statistic recomputation. Formal preflight, G8 status, and claim reauthentication reject archive mode; add an authenticated executable adapter before formal launch. | `CODE_FAIL_CLOSED_EXTERNAL_ADAPTER_REQUIRED` |
| `FORMAL-INPUT-001` | P0 external input | Candidate data now pass live and relocated zero-tolerance verification under the approved LLVM 22.1.8 runtime. Independent RT, model, license and G8 inputs remain absent. | Candidate-data readiness no longer substitutes for G0-G2 or external evidence. | Obtain all remaining independent inputs and destination-host approval, then run the same-run formal gate chain. | `BLOCKED_REMAINING_EXTERNAL_INPUTS_REQUIRED` |

## Current launch boundary

- `PACKAGE_INTEGRITY`: candidate bundle and relocated replay `PASS`; PR exact-head CI pending.
- `SOFTWARE_READY`: `LOCAL_TARGETED_PASS_PR_CI_PENDING`.
- `PROTOCOL_READY`: `PASS` (`A + R1`).
- `M4_DATA_PRODUCTION_READY`: `PASS`.
- `EVIDENCE_REGISTRY_READY`: `YES`.
- `FORMAL_CANDIDATE_READY`: `PASS_CANDIDATE_NOT_CLAIM`.
- `FORMAL_CANDIDATE_DATA_READY`: `PASS`.
- `FORMAL_INPUT_READY`: `BLOCKED`.
- `FORMAL_TRAINING_READY`: `NO`.
- `LAUNCH_READY`: `BLOCKED`.
- `SCIENTIFIC_EVIDENCE`: `NOT_ASSESSED`.
